//Strings in javascript can be defined using either single or double quotes

let someString = "this is a string";
console.log(someString);

//* String Formatting in JavaScript
/*
String Formatting in JavaScript is very simple. Just use back-ticks instead of
quotes when defining a string. 

SYNTAX for printing the value of a variable within a string. 
? console.log(`Value of <variable-name> is ${<variable-name>}`)
*/

let a = 2; 
console.log(`The value of the variable 'a' is ${a}`);


