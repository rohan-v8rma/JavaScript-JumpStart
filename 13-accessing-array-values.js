/*
Zero indexing is followed in JavaScript which is why when we try obtaining the
element at index 1 in an array, we get the second element of the array
*/

let food = ['apple', 'pizza', 'pear'];
console.log(food[1]);